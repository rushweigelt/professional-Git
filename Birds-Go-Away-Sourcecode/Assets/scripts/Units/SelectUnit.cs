using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class SelectUnit : MonoBehaviour {

    public GameObject mouseAt;

    public void unitSelection()
    {
        //find map script so we can set Selected Unit
        GameObject board = GameObject.Find("map");
        Map_v11 map = board.GetComponent<Map_v11>();

        //find mouseInputControl so we have an idea of where mouse is
        GameObject mouse = GameObject.Find("Mouse");
        MouseInputControl = mouse.GetComponent<MouseInputControl>();

        

    }

}


*/